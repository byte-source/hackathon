# hackathon

npm install nodemon bower -g //install nodemon and bower globally

npm install // install the modules defined in package.json

bower install // install static modules defined in bower.json

To start server

npm start
